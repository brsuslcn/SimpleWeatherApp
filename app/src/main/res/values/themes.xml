<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleWeatherApp" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/violet</item>
        <item name="android:navigationBarColor">@color/violet</item>
        <item name="android:windowBackground">@color/violet_50</item>

    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:navigationBarColor">@color/violet</item>
        <item name="android:windowBackground">@color/violet_50</item>
    </style>

    <style name="Theme.SimpleWeatherApp" parent="Base.Theme.SimpleWeatherApp" />
</resources>